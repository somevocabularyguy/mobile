import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 200,
    width: '100%',
    borderWidth: 2,
    borderColor: '#d09c00',
  },
})

export default styles;